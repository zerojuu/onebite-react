// 배열
// 여러 개의 값을 순차적으로 담을 수 있는 자료형

// 1. 배열 생성
let arrA = new Array(); // 배열 생성자
let arrB = []; // 배열 리터럴

// 배열 안에는 어떤 타입의 값이든 담을 수 있음
let arrC = [1, 2, true, "hello", null, undefined, () => {}, [], {}];


// 2. 배열 요소 접근
let item1 = arrc[0];    // 배열 인덱스는 0부터 시작
let item2 = arrC[1];

// 배열 요소 수정
arr[0] = "hi";
